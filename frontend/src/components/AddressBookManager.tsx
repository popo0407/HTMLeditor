import React from 'react';

// アドレス帳管理の将来拡張用プレースホルダー
// 現状は未実装だが isolatedModules 対応のためモジュール化

export const AddressBookManager: React.FC = () => {
	return <div style={{display:'none'}} data-placeholder="address-book-manager" />;
};

export default AddressBookManager;
