# CORS設定（フロントエンドからのアクセス許可）
# カンマ区切りで複数指定可能
# 開発環境の場合
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# IIS環境の場合（例: サーバーIP が 192.168.1.10 の場合）
# CORS_ORIGINS=http://192.168.1.10,http://192.168.1.10:3000,http://localhost:3000

# 本番環境の場合（例: ドメイン名がある場合）
# CORS_ORIGINS=https://yourdomain.com,http://192.168.1.10

# セキュリティ注意: 
# 本番環境では "*" (全許可) は使用せず、必要最小限のオリジンのみを指定してください

# メール送信設定
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SENDER_EMAIL=your-email@gmail.com
SENDER_NAME=HTML Editor

# Microsoft 365 / Teams向け設定例
# SMTP_SERVER=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_USERNAME=your-work-email@yourcompany.com
# SMTP_PASSWORD=your-password-or-app-password
# SENDER_EMAIL=your-work-email@yourcompany.com
# SENDER_NAME=議事録システム

# Teams チャネル設定のヒント:
# 1. Teamsでチャネルのメールアドレスを取得
# 2. アドレス帳に登録（例：teams-project, teams-sales）
# 3. 件名自動生成機能を活用

# 本番環境では.envファイルにこれらの設定をコピーして実際の値を設定してください
# .envファイルは.gitignoreに含まれているため、機密情報が公開されることはありません
